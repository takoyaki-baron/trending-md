---
title: エッジ / ローカル推論エンジン
topic: edge-inference
created: 2026-08-13
---

# エッジ / ローカル推論エンジン（2026年8月）

極小のハードウェアで巨大モデルを動かすプロジェクト群。共通の手法：MoEのスパース性を活用——
小さな共有コアをRAMに常駐させ、ルーティングされたエキスパート重みを必要に応じてディスクから
ストリーミングする——モデル全体を量子化する代わりに。

## パターン
MoEモデルはトークンごとのアクティブパラメータ数が小さく、大部分のエキスパートは遊休状態。
それらのエキスパートをSSD/NVMeからストリーミング（LRU/LFUキャッシュ付き）することで、数兆
パラメータモデルを消費者向けハードウェアのワークロードに変える。「ゼロ量子化、ゼロ蒸留」が
共通のうたい文句。

## プロジェクト
- **kimi-k3-in-c** — `FareedKhan-dev/kimi-k3-in-c`、Apache 2.0。176KBのC99バイナリが8.24GB RAMで
  Moonshot Kimi K3（2.78Tパラメータ）を実行。MXFP4パックされたエキスパートをNVMeからストリーミング、
  16/896エキスパートがアクティブ、O_DIRECTトランクストリーミング、エキスパートLRUキャッシュ。
  PyTorch参照実装とバイト単位で同一。
- **TurboFieldfare** — `drumih/turbo-fieldfare`、Apache 2.0。Swift+Metalエンジンが~2GB RAM
  （Apple Silicon）でGemma 4 26B-A4Bを実行。~1.35GBの共有コア常駐、レイヤーごとの16スロット
  LFUエキスパートキャッシュ。
- **Ling-3.0-tiny** — `inclusionAI/Ling-3.0-tiny`（Ant Group Bailing）、MIT。7.9B MoE（1.3Bアクティブ）、
  KDA:MLA 3:1ハイブリッドアテンション、128エキスパート。M4 Pro MacBookで~90 tok/s、<100msファーストトークン。
- **Muse Glimmer** — `meta-models/Muse-Glimmer-30B`（Meta）、Apache 2.0。Muse Spark 1.2から蒸留した
  30B、~17GB 4-bit量子化、RTX 5090でDFlash投機的デコーディングにより233 tok/s。
- **Needle 2** — `cactus-compute/needle`（Cactus Compute）、Apache 2.0。45Mパラメータ → 14MB C++
  バイナリ。MLP層なし（Walsh-Hadamard変換）、ハッシュ化n-gramテーブル。Raspberry Pi 5で500–800 tok/s。
  Pebble Index 01スマートリングに搭載。
- **h3.c** — `antirez/h3-metal`、MIT。Apple SiliconでMiniMax H3オムニモーダルを動かすC/ObjC + Metal
  エンジン。safetensorsからのmmapロード、`--ssd-streaming`がDiTメモリを36.5→2.0 GiBに削減。

## メモリ管理の比較

「MoEスパース性」という共通ラベルの下に、実は2つの異なる戦略が隠れている。分けて考える価値が
ある——最適化している制約が違い、失敗の仕方も違う。

**A. ストリーミング + キャッシュ**（kimi-k3-in-c、TurboFieldfare、h3.cの`--ssd-streaming`）——
共有コアを常駐させ、ルーティングされたエキスパートを必要に応じてSSD/NVMeからストリーミングし、
ホットなエキスパートをキャッシュする。エキスパートがいくつあってもメモリフットプリントは
平坦なまま。コストは、ルーティング変更後の最初のトークンでのキャッシュミス。

- **kimi-k3-in-c** — 最大規模（2.78T → 8.24GB RAM）。エキスパート**LRU**キャッシュ、`O_DIRECT`
  トランクストリーミング、16/896エキスパートがアクティブ。PyTorch参照実装とバイト単位で同一、
  というのが際立つ主張。
- **TurboFieldfare** — 最小フットプリント（~2GB）。レイヤーごとの**16スロットLFU**キャッシュ、
  ~1.35GBの共有コア常駐。LFU（LRUではない）を使うのは、アクティブエキスパート集合が小さく、
  レイヤーごとにホットだから。
- **h3.c** — 汎用メカニズムをフラグ（`--ssd-streaming`）として公開し、LLMだけでなくDiT
  （拡散）スタックにも適用——このトリックがモダリティ非依存であることを証明。

**B. アクティブ集合を縮める**（Ling-3.0-tiny）——*トークンごとのアクティブ*フットプリントを
十分小さくし（7.9B中の1.3B、KDA:MLA 3:1ハイブリッドアテンション）、全体をRAMに収める。
ディスクストリーミングは一切行わない。総パラメータ数ではなく、レイテンシと決定的なファースト
トークン時間（<100ms）を最適化。

**再利用可能な洞察：** エンジン選択は*規模*（Aは任意の数のエキスパートをストリーミングできるが
キャッシュミスのコストを払う）と*レイテンシ*（BはミスしないがRAMに収まる量に上限がある）の
トレードオフ。キャッシュポリシー（LRU vs LFU、レイヤーごと vs グローバル）がA戦略エンジン同士を
分ける調整可能なノブ。この2戦略が融合するかどうか——より大きなハードウェアでオーバーフローした
エキスパートもストリーミングする「小さい常駐コア」モデル——に注目。

## オンデバイスVLM（第3の戦略、8月15日）

- **LFM2.5-VL-3B** — `LiquidAI/LFM2.5-VL-3B`、lfm1.0ライセンス。約3.1Bの視覚言語モデル（LFM2.5-2.6B
  バックボーン + SigLIP2 NaFlexエンコーダ）で、GUIエージェントのニッチ向け——27Bモデルを載せられない
  スマホ/ラップトップ上でローカルに画面を読み、オブジェクトを接地する。Apple M5 Maxで228 tok/s、
  Galaxy S26 Ultraで約20 tok/s（3.3GB未満）；ScreenSpot-v2 80.7、RefCOCO P@1 87.9、ChartQA 81.3、
  16言語。公式のGGUF/ONNX/MLX量子化が同梱。

これはストリーミング+キャッシュ（A）でもアクティブ集合の縮小（B）でもない——*小さな密モデル +
公式量子化*の経路で、上記のMoEストリーミングエンジンと相補的。オンデバイス推論は今や3つの戦略に
広がる：巨大なMoEをディスクからストリーミングする、アクティブ集合を縮める、あるいは一次量子化
付きの小さなモデルを出荷する。

## レイヤーストリーミングによるファインチューニング（8月16日）

「凍結ベースをストリーミングする」トリックは、推論だけでなく訓練にも広がった。**Soup**
（`MakazhanAlpamys/Soup`、Apache-2.0）はローカルファインチューニングのハードウェア下限を下げる：
1つのYAMLがSFT/DPO/KTO/ORPOと20以上の手法を駆動し、その **layer streaming** は凍結ベースをシステム
RAMに置いたまま、1つずつデコーダ層をGPUへストリーミングする——そのため **8Bモデルを4GBのノートPC
GPUでLoRAファインチューン**できる（RTX 3050で119.6 tok/s、ピークVRAM 3.32GB）。結果は9アーキテクチャ
で常駐GPU参照実装と**ビット単位で一致**することをCIテストで検証。A戦略（ストリーミング+キャッシュ）
と同じ形を*訓練*パスに適用したもの：凍結パラメータはVRAMに常駐する必要がない。ベータ：transformers +
通常のLoRAのみ（GRPO/PPOは除外——生成時に全層を読み直す）。Axolotl/LlamaFactory設定を移行可能。

## Apple Neural Engineでのオンデバイス訓練（08-17 04:03）

MIT系プロジェクト群がAppleのプライベートANE API（`_ANEClient`、`_ANECompiler`）をリバースエンジニアリングし、
**推論だけでなく訓練を**Neural Engine上で実行——CoreMLもMetalも不要：

- **ANE**（`maderix/ANE`）—— 概念実証：Stories110Mで順伝播 + 逆伝播、約91–115 ms/ステップ。
- **Orion**（`mechramc/Orion`）—— グラフコンパイラで「Delta Compilation」（重み更新が8.5×高速）、110M
  トランスフォーマーを約22分で1,000ステップ安定訓練。
- **ANEForge**（`sbryngelson/ANEForge`）—— pipインストール可能なPythonバインディング（約75 tok/s、テストした
  モデルでGPUより8–16×省エネ）。

信号：これはテーゼ3の「凍結ベースをストリームする」流れを*推論*から、真に新しいオンデバイス**訓練基盤**へ
拡張する——AppleのANEは設計上推論専用だった。プライベートAPIと約5–9%の利用率のため、当面は研究グレード。

## 「このマシンで動くか」がツールになる（8月18日）

オープンモデルが増えるにつれ、*インストール*問題は「どう動かすか」から「収まるか、どの量子化か」へ移った——
2つのプロジェクトがその答えを製品化する：

- **llmfit** —— `AlexsJones/llmfit`、MIT、約32k stars、Rust CLI。RAM/CPU/GPU/VRAM/バックエンドを検出し、メモリ
  帯域モデル（約80 GPUのルックアップテーブル）で数百モデルをメモリ適合・推定速度・品質・コンテキストでスコア
  リングし、収まる最高の量子化を選ぶ。MoEモデルは**アクティブ**パラメータで正しく換算（Mixtral 8x7Bは約23.9GB →
  約6.6GB）。`llmfit bench` が実測tok/sを測り、ユーザーがPRで推定値を置き換える。`llmfit recommend --json` は
  スクリプト/エージェント向け、`llmfit plan` は「このモデルにはどんなハードウェアが必要か」を逆引きする——
  ハードウェア検出 + 量子化選択がワンコマンドで、エージェントからスクリプト化できる答えになった。
- **omlx** —— `jundot/omlx`、Apache-2.0、約19k stars、SwiftUI macOSアプリ（vllm-mlx由来）。MLXでApple Silicon上
  にLLM/VLMをネイティブ実行し、localhostにOpenAI/Anthropic互換APIを公開。目玉は**2層KVキャッシュ**——ホットRAM層
  + コールドSSD層（safetensorsで永続化、再起動後も保持）——に加え、連続バッチング、マルチモデルLRU退避、RAM-8GB
  未満メモリ強制、MCP/構造化出力対応（LLM、VLM、OCR、埋め込み、リランカー、オプションで分散マルチMac推論）。
  Apple Siliconのユニファイドメモリはローカルモデルの最良の予算ホストで、omlxはそれを本物の（SSDバック・バッチ
  処理）サーバーに変える——「Mac＝推論ノード」への一歩。

信号：エッジ推論の話に、エンジンだけでなく*選択*と*配信*の層ができた——llmfitは「どのモデル + どの量子化がこの
マシンに収まるか」、omlxは「それを常駐サーバーとして提供する」に答え、いずれもローカルファースト。

## 実測予算へのフィッティングがプリセット圧縮を置き換える——RAMが安くなくなる中で（8月19日）

3つの独立プロジェクトが2週間のうちに*同じ*再フレーミングへ収束した：圧縮プリセットを選ぶのをやめ、実際に
測定したバイト数に対して割り当て問題を解くのだ。

- **Shoehorn**（MIT、Rust、8月13日作成）——量子化選択を反転させる。マシンを無視したプリセットを選ぶ代わりに、
  「実際に持っているメモリから始め、推論自体が必要とする分を差し引き、残りに対してテンソルごとの混合精度
  割り当てを解く」。報告されたフィットは極端で、「予算の**99.99%**を日常的に使用、時にはバイト単位」という、
  `unsloth/Qwen3-4B-GGUF`の実例では**519.2 MiBの予算に対して519.2 MiBを使用——99.998%使用、余裕13 KB**。
  量子化器はRustでゼロから書かれ（llama.cppのコードはリンクされない）、標準の**GGUF v3**を出力し、llama.cppは
  推論バックエンドとしてのみ使われるため、下流は何も変わらない。`shoehorn ui` がマシンを測定し、フィットを
  ストリーミングし、チャット前にパープレキシティコストを報告する。macOS Apple Silicon、Linux x86-64
  （NVIDIA/AMD）、Windows x86-64（NVIDIA）を対象とし、8 GBから128 GBのプロファイル、4k–32kのコンテキストに対応。
  **非常に若い——確認時点で37 stars** なので、99.998%という数字は独立した結果ではなく作者のデモとして扱うこと。
- **Linux VRAMオーバーコミット** —— Valveの契約者 **Natalie Vock** が、GPUメモリ圧力下でLinuxがフォアグラウンド
  ゲームのVRAMをシステムRAMへ追い出すのを止める作業を出荷した。**`dmem` cgroupコントローラ**（`dmemcg`、
  Maarten Lankhorst/Intel + Maxime Ripard/Red Hatと共同開発、すでにメインライン入り）の上に構築され、6つの
  カーネルパッチと2つのユーザースペースヘルパー——`dmemcg-booster` とKDE Plasma「Foreground Booster」フォーク——
  を追加し、フォアグラウンドアプリがVRAMを勝ち取り、バックグラウンドアプリが先に追い出されるようにする。AMD
  `amdgpu` とIntel `xe` をカバー；**NVIDIAには同等のメカニズムがない**。実例：バックグラウンドアプリが
  **7.4 GB**必要なタイトルに対して8 GBカードの**6.1 GB**しか残さず、パッチが1 GBを返還する。CachyOS
  （Linux 7.0rc7-2+）と `linux-dmemcg` AURパッケージで今すぐ利用可能。
- **llmfit**（上記）——同じ形が一段上にある：マシンを測定し、収まる最高の量子化を選び、MoEを*アクティブ*
  パラメータで換算する。

**これを急務にする相殺要因——メモリは安くならなくなった。** TrendForce（8月17日）：ドイツのDDR5小売価格指数は
8月に前年比**445%から486%へ上昇**——一般的なキットは**昨年の価格の約4.9倍**——一方、深圳の華強北市場では
**DDR5 24Gbが前週比+14.29%で$48**、16Gbは$40、**DDR4 8Gb 3200は前週比+12.82%で$22**となった。TrendForceは
**3Q26にサーバーDRAM契約価格が前期比13–18%上昇**すると予測し、市場は供給不足とし、サーバーDRAM不足は2027年
まで続くと見込む。Tom's Hardwareの小売データポイントは**128 GBのDDR5が$3,399**（見出しのみ——本文はペイ
ウォール）。原因：AIデータセンターとHBMの需要が汎用部品からファブ容量を奪っていること。

信号——このファイルの2つの半分が今、互いに引っ張り合う。**MoEスパース性 + ディスクストリーミングがモデルの
下限を下げた**（テーゼ3の当初の主張）；**DRAM価格がちょうどマシンの下限を引き上げた**。だから最適化圧力は
「モデルを小さくする」から「**手持ちの正確なバイトを使い切る**」へ移った——それこそが、フィットソルバー
（Shoehorn、llmfit）とOSレベルのデバイスメモリQoS（`dmemcg`）が同じ時期に現れた理由。cgroupの仕事がゲームを
超えて重要なのも同じ理由：ローカルモデルとデスクトップ上の他のすべてとの間でVRAMを調停する、最初のメインライン
のプリミティブだからだ。

## Unslothがデスクトップアプリに——実行と訓練が1つのローカルツールに（8月19日）

`unslothai/unsloth`（Apache-2.0、**73,546 stars**、8月18日プッシュ）は静かにカテゴリを変えた：リポジトリ説明は
今や「Local UI to run and train LLMs and diffusion models」とあり、**Unsloth Desktop** はWindows/macOS/Linux向け
に速いリリース列車（v0.1.70-beta → v0.1.800-beta、8月11–14日）で出荷され、ノーコード訓練、RAG、MCP、リモート
Cloudflareアクセスを備える。最新リリースはDynamic GGUFsとNVFP4量子化により**約17 GB RAMでQwen3.8-27Bをローカル
実行**し、より低いVRAMでGGUF推論が約10%高速と主張し、「Fast FP8 10× faster MiniMax-H3 inference（30に対して
3分）」をモデル分割で小さなGPUに収める。AMD RDNA 3/4 + Strix Halo対応、Macでのメモリベースのコンテキストサイズ
設定、モデルごとの`llama-server`引数、外部プロバイダ向けのツール呼び出し + ウェブ検索も追加された。

トリガーは、2週間のうちに3つのモデル投入が1つのツールに着地したこと——Desktopのローンチ（8月11–13日）、Meta
Muse Glimmer対応（8月10日）、Qwen3.8対応（8月14日）。信号：Unslothは*インポートする微調整ライブラリ*だった。
今や同じハードウェアでモデルを実行**し**適応**する**、MCPも組み込んだローカルファーストのGUI——notebookを
開いたことのない人々のために「モデルを試す」と「モデルを適応させる」の間のギャップを埋める。Shoehornやllmfit
と合わせ、ローカルスタックは選択・フィッティング・配信・適応をすべて普通のデスクトップソフトウェアとして持つ
に至った。

## Ling-3.0 ベースチェックポイントが MIT に + ドメイントークンの教訓（08-21 04:03）

- **Ling-3.0 ベースチェックポイント**——上記の `Ling-3.0-tiny` エントリに研究級の兄弟が加わった：Ant
  Group/inclusionAI が `Ling-3.0-tiny-base`（7.9B/1.3Bアクティブ）と `Ling-3.0-flash-base`（124B/5.1Bアクティブ）、
  さらに**事前訓練・中間訓練・WSMマージ段階にまたがる6つのチェックポイント**を **MIT** で公開。中間訓練段階は
  研究者が通常見られないもの——フロンティア隣接モデルでの継続事前訓練とMoEアブレーションが可能になる。
- **RollTab**——iPhone 上で MIDI ピアノ演奏をライブで継続する 125M の decoder-only transformer（Core ML、INT8、
  iPhone 15 で約108 notes/s）。転用できる教訓はトークン化：**5つのカテゴリカルフィールド（イベント種別、音高、
  開始デルタ、音価、ベロシティ）を運ぶ単一の NOTE トークン**をフィールドごとでなく音符ごとに1回走らせることで、
  125M がリアルタイムに感じられる。ドメイン特化トークン化が力ずくのスケールに勝つ——「持っているバイトを正確に
  使う」のオンデバイス版。

## 投機的デコード + スパース長文脈（08-23 04:03）

- **Liquid AI DSpark**——自己完結型の投機的デコード下書きチェックポイント（1.2B/2.6B/8B-A1B）が LFM2.5 を**貪欲出力が同一
  であることを保証**しつつ加速（下書きトークンはターゲット分布と一致したときのみ受理）：H100 で最大 **3.18×**（MATH500 で
  428→1362 tok/s）、M4 Max で **2.87×**（136→389 tok/s）、マルチツール関数呼び出しで平均レイテンシ57%削減、llama.cpp + SGLang
  を初日からサポート。純粋な ~3× 高速化・品質損失ゼロで、データセンターから MacBook まで——「持っているバイトを正確に使う」
  最適化に*無損失保証*の投機的変種が加わった（thesis 3）。
- **KeysAndValues**（AWS、arXiv 2608.19920）——長文脈**スパース注意**のファインチューニング手法で、単一 A100 40GB 上で任意の
  KV キャッシュ方針に適用可能。モデルを方針と共適応させ、正確なシーケンス並列注意で訓練したモデルをしばしば上回る。H2O カーネル
  + OSS ライブラリを同梱。長文脈スパース微調整を控えめなハードウェアで非現実的にしていたシーケンス並列前提を取り除く。
- **既知リポジトリ、新事実。**`jundot/omlx`（~20.3k stars）は DeepSeek-V4-Flash M2-Ultra カーネルを追加し、ANE コンパイル
  メモリを 35.8GB→4.7GB に削減（0.6.3rc2）。`AlexsJones/llmfit`（~33.6k）は「測って共有する」tok/s PR ループを継続。
  両者とも 08-18 に既出——重複排除ウィンドウ拡大（[[agent-stack]] 参照）がこれらを新発見ではなく更新として扱うべきだった。
- **FreeToken**（arXiv **2608.16157**、2026-08-17投稿；`FlashML-org/FreeToken`、Apache-2.0、2,824★、2026-07-20作成、
  毎日プッシュ）——*「Efficient Edge-Native MoE Serving with Bandwidth-Adaptive Execution」*（Shuo Yang、Xiaoze Fan、
  Melissa Pan、Haocheng Xi、Zhe Wang、Shanlin Sun、Kurt Keutzer、Song Han、Matei Zaharia、Chenfeng Xu、Ion Stoica）。
  本ファイルのテーゼのこれまでで最強の実例であり、それを一般化する。先行研究が*固定*プランに対してエキスパートを
  ストリーミングしたのに対し、FreeTokenは個人マシン全体——GPU、CPU、RAM、PCIe、ディスク——を「統一された伸縮可能な
  推論プラットフォーム」として扱い、固定オフロード戦略の代わりに「計算とモデル状態を実際に利用可能なリソースへ継続的に
  マッピング」し、モデルのレイアウト/ロード、エキスパート常駐、CPU–GPU実行、エージェント状態の再利用、実行時メモリ管理を
  共設計する。要旨ページで検証済み：**8 GBのノートPC GPUで35B**、**ゲーミングデスクトップで284B**、**単一ワークステーション
  GPUで753BのGLM-5.2**、そして**20以上のMoEモデル**。*速度向上は慎重に読むこと：* llama.cpp / Ollama / KTransformers /
  MoE-Infinityに対する1.3–2.1×の平均デコード向上は**要旨ページにはない**——PDFにあり、要旨で検証された数値ではなく論文の
  主張として引用すべき。
  **最も鋭い一文は数字ではなく動機。** FreeTokenは適応性を、エージェントワークロードが「実行パターンを継続的に変える」——
  プリフィル主体のツール読み、デコード主体の推論、状態再利用のバースト——ため、静的なオフロードプランは構造上たいてい
  間違っていると論じて正当化する。ローカル配信は今やチャットではなく*エージェント的*な分散に対して設計されており、それこそ
  最適化目標が再び動いた理由：08-19は「測定された予算への適合」（静的）、これは「いま現在持っている予算への適合」（動的）。
  DRAM価格ショックで始まった弧を閉じる——バイトを買えないなら、それをスケジュールせよ。
- **FlashPrefill V2**（arXiv 2608.19758；`qhfan/FlashPrefillv2`、Apache-2.0）——極端なスパース性での近似誤差を抑える
  平均補正項付きのブロックスパース・プリフィル注意、加えてwarp特化とピンポンパイプライニング（FP8/BF16）を持つPackGQAスパース
  演算子、ページングKVキャッシュ、連続バッチング、ドロップインの **SGLang** バックエンド。H20で128Kコンテキストにおいて
  **FlashAttention-2比で最大47.26×（FP8）/ 27.19×（BF16）**を報告。**鮮度/信頼性の注意、一次確認済み：** リポジトリは
  2026-08-19作成で、読んだ時点で**8スター**——生後2日のアーティファクトに47×の見出し、第三者再現なし。正しい姿勢は
  InferenceX流（[[frontier-models]]参照）：これほど大きなカーネル主張は、事実として扱う前に常設で継続稼働するハーネスに
  載せるべき。軸にも注意：FreeTokenは*エッジ*を、FlashPrefill V2は*データセンター*の長文脈配信を最適化する。同じ年、
  ハードウェア曲線の両端、そして机の上のマシンに関するのは前者だけ。

## Daedalus-150M——CPU 階層のために KV キャッシュを設計で消す（08-24 12:03）

`Daedalus-150M`（arXiv 2608.20210）は CPU 推論向けに構築された 150M パラメータの LM：18 ブロックのうち 6 つだけがフル
アテンションを使い、12 は「メモリが 2 タイムステップ幅」の短い畳み込みを使うため、ネットワークの 3 分の 2 は成長する
キャッシュを二度と読み返さない。59.9B トークン・4-bit 重みでゼロから訓練し、事前登録の 5 タスクベンチマーク（47.31 vs
42.20 の基準）で GPT-2 124M、Pythia-160M、OPT-125M、MobileLLM-125M を上回る——後者は 3×–1000× のデータを見ているのに——そして
2K コンテキストで同サイズの全アテンション対照より 1.76× 速くデコードする。要点は*アブレーション*：データもサイズも同じで
アーキテクチャだけが変わる——したがって KV キャッシュ（長文脈 LLM の主要メモリコスト）がそのレバーとして分離される。FreeToken
が生きた予算に合わせてエキスパートをストリーミングするのに対し、Daedalus は*もう一方*のメモリコスト——キャッシュそのもの——
を、ネットワークの大部分からアテンションを外すことで攻撃する。エッジの弧が延びる：KV キャッシュは所与ではなく、CPU/エッジ階層が
おおむね辞退できる設計選択である。

## Second Thought —— 待機窓での推論（08-25）

**Second Thought**（arXiv 2608.13667、SMU）は ReAct エージェントの「推論待機窓」——ツール実行と観測を待つ時間——を活用
する：各 Thought フェーズが終わった瞬間に**4 つの補助推論ブランチ**（検証、想起、リハーサル、フォールバック）を分岐し、
メインループと並行してデコード、観測が届いた時点でマージする。訓練不要。3 ベンチマーク × 3 LLM でメインスレッドの
デコードを最大 43%（平均約 20%）削減し、計算量を揃えた対照に対して 1.3–3.2× 少ない逐次デコードでより高い Pass@1 を
達成。エッジ/量子化のトリックではない——エージェントランタイム層の「待つ間に考える」であり、ユーザーが感じる遅延や
再訓練なしに推論をスケールさせる；ツール I/O で待機するあらゆるランタイムに直接関係する。


## Apple の 2nm 転換——512 GB / 1.2 TB/s のオンデバイス型フロンティア級推論（08-26 04:03）

ローカル推論の上限が引き上げられた。Apple は **M6**——初の **2nm** チップ——を新しい Mac mini で発表（デュアル 16 コア
Neural Engine、AI 性能は従来 mini 比最大 4×、**$899**）。**M5 Ultra**（クアッドダイ UltraFusion、最大 36 コア CPU / 80 コア GPU）
は Mac Studio に搭載——**512 GB ユニファイドメモリ + 1.2 TB/s 帯域**で「数千億パラメータ」のオンデバイスモデルを可能にし、
LLM プロンプト処理は M1 Ultra 比最大 9.8×。Mac Pro は廃止され、Studio が最上位デスクトップに。9月22日 macOS 27 で出荷。
約 4.3–4.5× の AI 数値は Apple 独自のもの。価格跳上げ（mini $899 / Studio $5,499）は DRAM コスト環境を反映
（上記メモリ経済ノート）。thesis-3 のローカル推論ターンは、フロンティア級ウェイトを常駐させられるコンシューマーに近いマシンを
手に入れた——帯域適応型サービング（FreeToken のデスクトップ 284B / 単一ワークステーション 753B）のハードウェア側が理論でなくなる。

## llama.cpp v0.3.0 + Perplexity Portable Computer — ローカルスタックの参照ランタイムと製品化されたローカルファーストエージェント（08-26 12:03）

- **llama.cpp v0.3.0（ggml-org）— ローカル推論の参照ランタイムとしては久々の 0.x メジャーバンプ。** `mtmd`
  マルチモーダルライブラリが **dots3-note のビジョン/オーディオ**（新しい DSA-ISWA KV キャッシュ型）、ffmpeg 経由の
  WebP デコード、Pillow 準拠のリサイズ、`moov` atom がファイル末尾にある動画の修正を追加；GLM-4.5-Air は MTP を獲得、
  DeepSeek 4 はテンソルスプリットモードを追加、コアは **ggml v0.22.0** へ（meta-backend テンソルスプリット、
  並列コンパイルの per-op Metal カーネル、真の非 in-place `ggml_clamp`）。マルチモーダル＋動画処理が、ほとんどの
  ローカル AI ツールが依存する単一バイナリに統合される — ローカル推論エコシステム全体（thesis 3）への一等信号の更新。
- **Perplexity Portable Computer — NVIDIA DGX Spark で「ローカルファースト、オプトインクラウド」を製品化。**
  NVIDIA と密接に協力して構築した Computer エージェントプラットフォームの完全オンデバイス版：ローカルモデル
  （Qwen 3.8 27B または Perplexity が事後学習した **PPLX 27B**）、エージェントハーネス、ツールルーター、コネクタ、
  OS レベルサンドボックスがすべてローカルで動作し、ローカル作業は**ゼロトークンクレジット** — 15+ のクラウド
  フロンティアモデルへのエスカレーションは明示的承認が必要で、ローカルファイルに触れないテキストのみの助言を返す。
  自社の Local Knowledge Work Bench で 82.6%（PPLX 27B で 85.4%）vs Pi 77.6 / Hermes 74.0、BrowseComp では Pi より
  約 70% 少ないトークン。追うべき論点：ローカルエージェントには汎用ではなく*共設計された*ハーネスが必要 —
  小モデルエージェント論争をハーネス設計問題として再枠組み化（thesis 12 のレバーをエッジへ）、上記 M6/M5 Ultra の
  ハードウェア上限に載る。
  **独立確認（08-26 12:27）：** このベンチマークは**依然ベンダー実行** — Perplexity は Local Knowledge Work Bench の
  オープンソース化を計画するも未実施、第三者再現も存在しない。共設計*メカニズム*には独立の裏付け：ハーネスプレミアム
  文献（thesis 12、arXiv:2605.30621）は弱モデルが汎用ハーネスを*ロード*できず遵守も失敗することを発見
  （skill-load 0.251、遵守 0.52→0.13）——まさに Portable Computer が挙げる「小モデルは汎用ハーネスの前提で耐えられない」
  という失敗。Perplexity 自身の内訳は Pi への ~12 ポイント差のうち ~5 をハーネススタック（ベース Qwen も Pi を ~5 リード）+
  PPLX 後訓練は 2.8 のみとする——ベンチマークがオープンソース化されるまでは方向性のある主張として扱い、仕様としない。
  DIY 再現経路（Ollama + Qwen3.8-27B + OpenCode、24 GB GPU）は存在するが独立ベンチマークではない。

## QAH + CarWatch + Groq 3 LPX (08-26 20:19)

- **QAH —— 量子化認識ヒーリングで 4-bit モデルが bf16 の元モデルに逆転（arXiv 2608.20953、Multiverse Computing）。**
  QAT/QAD で劣化した中間教師の代わりに、4-bit 学生を**元の全精度モデルから直接** KL ダイバージェンスで蒸留。
  GPT-OSS 120B → 60B → MXFP4 で、QAH 学生は **9 ベンチ中 7 つで bfloat16 の元モデルに並ぶか上回り**（AA-LCR 42.7 vs
  35.3、AIME 2025 76.3 vs 70.7、Aider 40.9 vs 38.2）、LiveCodeBench では 120B 教師に肉薄——重みとトークンあたり計算量は
  約半分。GPT-OSS 9B ではピーク時に QAT より約 7 倍速く、1,200 ステップでピークから約 2 点に留まり、QAT は約 19 点低下。
  オープンウェイト **HyperNova-60B**（Apache-2.0）として公開。**注意：** Multiverse 自社パイプラインの自社測定
  （プロプライエタリ圧縮、GPT-OSS のみ）——「bf16 を上回る」は再現すべき結果であり、独立の事実ではない。実測予算に
  合わせる量子化ターン（thesis 3）に「癒える圧縮」という変種が加わる：4-bit + 半パラメータで全精度に並ぶなら、
  オープンモデルの支配的サービスコストは下がる。
- **CarWatch —— Raspberry Pi 5 を完全オフラインの車載エージェントに（`ThinkOffApp/CarWatch`、AGPL-3.0、171★、Show HN）。**
  **Qwen3.6-35B-A3B** をローカル実行（約 14.3 GB 量子化、約 3.5 tok/s）。745 ページの取扱説明書を RAG、Bluetooth ELM327
  で OBD-II を読み取り、Home Assistant 経由で安全コマンド（ロック、閉窓）を実行。ハンズフリー音声は完全オンデバイス——
  連続 VAD → whisper.cpp → 接地応答。約 $100 のデバイスで 35B ローカルモデルという具体的な「ローカル AI」の終着点。
  読み取り専用 OBD-II と明示的に安全なコマンドの分離は、オンデバイスエージェントの健全な安全モデル。
- **NVIDIA Groq 3 LPX —— デコードエンジンが量産入り（Gemma 4 31B・100K コンテキストで約 3,400 tok/s）。** Hot Chips 2026 で
  NVIDIA は Groq 買収由来のデコード段 **LPU** チップ（Vera Rubin と補完関係）の量産を発表。Artificial Analysis の実測は
  **Gemma 4 31B・100K コンテキストで約 3,400 出力 tok/s**（zhidx は 100K で 3,431 tok/s 中央値、10K とほぼフラット、
  SPEED-Bench コーディング中央値 4,767）。1 ラックに LP30 アクセラレータ 256 個（128 GB オンチップ SRAM、640 TB/s
  scale-up、液冷）。**Nebius** が Token Factory 経由で最初のクラウドに。**マルチターンのエージェントワークロード（チャット
  ではなく）こそ推論の制約**というハードウェアの賭け——実測予算ターンと補完的な「デコードエンジン」。注意：見出し数字は
  フロンティア MoE ではなくオープンな 31B のもの。注意（08-26 20:19 検証済み）：見出し数字は **Artificial Analysis 実測だが、
  プライベートのプレリリースエンドポイント**（8/21）での値であり、本番 serverless ではない——独立評価者ではあるが、
  まだ本番計測ではない。4×/30× はベンダー予測。

## マスクはモデルではない + ALPHABET（08-27 04:15）

- **出荷済みハイブリッドモデルでの因果リーク——《マスクはモデルではない》（arXiv 2608.22876）。** この分野の既定の因果正しさ
  チェック——アテンションマスクの検査——は根本的に不十分。論文は**プレフィックス不変性**を形式化し、各層を採点する 1 ページ・
  2 回フォワードパスの監査を提供。**8 つの公開済みチェックポイント**を 192 回の注入故障試行でテストし、**2 つ**に実欠陥を発見：
  Zamba2 と Nemotron-H が再帰/スキャン成分の**チャンクスキャン境界**でまさに情報を漏らす。マスクは正しいのに、チャンク間の
  集約が漏れる——**「因果性はグラフレベルの性質。」** マスク検査は「何も検出しなかったが、我々の監査は 192/192 をすべて正確な層に
  特定した。」**なぜ重要か：** *出荷され広くダウンロードされた*オープンモデルでの因果リークは、事前訓練ウェイトに未来文脈の
  汚染があることを意味する——そして教訓は、現在出荷されているあらゆるスキャン/集約アーキテクチャ（**新しい DeltaNet/QSA
  ハイブリッド（Qwen3.8-Flash-Next）**や Kimi K3 の線形アテンションコアを含む）にも及ぶ。監査ツールは安価（1 ページ・2 回
  フォワード）。未解決の問いは、ハイブリッドが*出荷前に*監査されるかどうか（→ アジェンダ）。
- **ALPHABET——6,437 パラメータの線形時間系列モデルがベイズオラクルに迫る（arXiv 2608.24051）。** 直接バンク（特徴軌跡への
  再合成）、独立カスケードバンク、モーダルエネルギーとラグモーメントのみを読むアフィンヘッドにより、時間履歴を安定した複素
  「極モード」に圧縮——幅 D=64 の「明示的に監査可能な予測インターフェース」。ガウス制御タスクでは学習記述子が **ベイズオラクル**
  に迫る一方、生の自己共分散は偶然レベル。82 タスクレジストリで平均順位 **3.97**、9 ベースラインより**推論 5.02× 速い** /
  訓練 3.93× 速い。各モードエネルギーは二次スペクトルの周波数局所測定に対応。「超小型効率的モデル」トレンドの極端
  （Daedalus の KV キャッシュ除去と並ぶ）——監査可能な内部状態（モーダルエネルギーであってブラックボックス活性ではない）は、
  モデルが*なぜ*そう決めたかを知る必要がある制御タスクで真の差別化要因。
- **監査ツールは同じ週に製品化された——VIDRAFT AX-RAY（08-27 04:30、直接確認）。** 『The Mask Is Not the Model』の著者
  （VIDRAFT、韓国の AI 企業、CEO Kim Min-sik）はこの診断を **AX-RAY / FINAL-Bench Diagnostics** として製品化——公開の
  AI 安全カタログで **117 の診断項目**を 3 軸・11 の運用カテゴリ（プレフィックス不変性/因果リーク、チャンクスキャンとマスク
  整合性、KV キャッシュ経路整合性、実装間整合性）にわたり持ち、項目を法的・規制・倫理コンテキストにマッピングし、確認された
  因果リークをスコアに関わらず**ブロッキング欠陥**として扱う。韓国政府の「サイバーセキュリティ特化 AI 基盤モデル」開発プロジェクト
  の検証層として位置づけられている——監査は今や*規制*顧客を持つ。**監視、現時点での回答（08-27 04:30）：この時点で、
  Qwen3.8-Flash-Next（Gated DeltaNet + QSA）または GLM-5.3-Flash（スパース + リニア）に対する公開済みのプレフィックス
  不変性監査はない**——ラボによるものも第三者によるものも。根本原因は今やコードレベルのセンサス項目：
  `transformers` 5.7.0 の `modeling_zamba2.py` と `modeling_nemotron_h.py` は**出力**チャンク軸で置換後にリダクションする一方、
  `modeling_mamba2.py` は**入力**チャンク軸でリダクションする——しかもこの欠陥は高速カーネルが無い場合（CPU/CI のスローパス）に
  のみ発火する。監査が十分安価な以上、新ハイブリッドのリリースにそれが無いこと自体がシグナルになっている。

## colibri + Baidu Unlimited-OCR——GPU 不要の MoE と定数 KV デコーダ（08-28 12:15）

- **colibri（`JustVugg/colibri`、Apache-2.0、純 C、26.3k★）——現時点で最強の「GPU 不要フロンティア」エンジン。** VRAM・RAM・NVMe を 1 つのメモリ階層として扱う：744B MoE の約 19,456 のルーティングエキスパートはディスク上（約 370 GB）に置かれ、学習済みホットピンを持つレイヤー毎 LRU キャッシュ、バッチユニオン読み取り、`O_DIRECT`、デュアル SSD ミラーリングを通じてオンデマンドでストリーミングされる。GLM-5.2（744B）、Kimi K3（2.8T）、Inkling（975B）、DeepSeek-V4-Flash、Qwen3.6、OLMoE を実行——「どれも GPU を必要としない」；速度はディスク律速で、GPU は助けにしかならない。v1.8.0、活発なメンテナンス（オープン issue 77、PR 40）。エキスパートストリーミングは、フロンティア MoE 推論にデータセンターが必要という前提を崩す——2.8T パラメータモデルをラップトップで扱えると主張可能にする同じ圧力（テーゼ3、kimi-k3-in-c / FreeToken と並ぶ）。
- **Baidu Unlimited-OCR（`baidu/Unlimited-OCR`、MIT、24.7k★）——定数 KV キャッシュによるワンショット長文書パース。** DeepSeek-OCR スタイルのパイプラインのデコーダアテンション層すべてを Reference Sliding Window Attention（R-SWA）に置換：グローバル可視の参照ビジュアルトークンセグメント + 128 トークンのスライディングデコードウィンドウにより KV キャッシュを一定に保ち、数十ページをメモリをリセットするページ毎ループではなく単一フォワードパスで文字起こしする。3B 総計 / 500M アクティブの MoE デコーダは 1024×1024 の PDF ページを 256 ビジュアルトークン（16×）に圧縮し、単一ページ（「gundam」）と複数ページ（「base」）モードを持つ。OmniDocBench v1.5/v1.6 の単一ページエンドツーエンドパースで SOTA に到達；著者らは R-SWA が ASR と翻訳に一般化すると主張。「ソフト忘却」が KV 成長の壁への実際の修正——ラッパーではなく一般アテンションパターン（テーゼ3、Daedalus-150M のキャッシュ除去と並ぶ）。

## ODS——ローカル AI インストーラーが独立カテゴリになる（09-01 12:22）

- **`Osmantic/ODS`（Apache-2.0、5.6k★、v2.6.0 stable）**——`curl | bash` のインストーラー（Windows では PowerShell ブロック、
  Docker 必須）で完全なローカルスタックを組み立てる：llama-server、Open WebUI、LiteLLM、Whisper、Kokoro TTS、Hermes
  エージェント、n8n、Qdrant、SearXNG、ComfyUI。NVIDIA / AMD（Strix Halo 統合メモリ含む）/ Intel Arc / Apple Silicon / CPU を
  自動検出し、VRAM/RAM 予算に合うモデル階層を選び、「bootstrap モード」は本物のモデルがバックグラウンドでダウンロードされ
  ホットスワップする間、2 分以内に 1.5B モデルを提供し始める。各サービスは `ods` CLI が管理するドロップイン拡張（manifest +
  compose ファイル）；デフォルトでローカルファースト、クラウド/ハイブリッドは任意。統合コストこそが商品——ローカル AI
  インストーラーは、新ハードウェアの波（Strix Halo、Mac Studio クラスタ）が人々にそれを向けるマシンを与えたまさにその瞬間に
  カテゴリになりつつある。注意点：3.2k コミットに対し約 1.4k のオープン PR は異例なメンテナンス形態；「主権的人権」的
  フレーミングはプロジェクト自身のマーケティング；組み立てられたスタックのサードパーティベンチマークなし。

## Baseten の効率フロンティア — どの技術がトレードオフし、どの技術がそれを消すか（09-02）

- Philip Kiely がポートフォリオ理論を推論エンジニアリングに輸入：あらゆるデプロイはレイテンシ–スループットの
  効率フロンティア上にあり、技術は**フロンティアに沿って移動する**もの（バッチサイズ、テンソル/エキスパート/
  アテンションデータ並列）と、**フロンティアを押し出す**もの（量子化 MXFP4/NVFP4、投機的デコード EAGLE-3、
  prefill/decode 分離）に分かれる — フロンティアの利得は複合する（2× ハードウェア × 2× ソフトウェア ≈ 4×）。
- 目立つ但し書きが正直な部分：**ベンチマーク無し**の概念的分類学；フロンティアは「非常にギザギザ」で、閾値は
  経験的スイープでしか発見できない；フレーミングは GLM-5.3/Kimi K3 クラスのモデルが KV キャッシュ再利用と
  KV アウェア ルーティングで agentic coding をする前提。量子化には独自の注記：それは無料の勝利ではなく*新しい*
  品質軸を開く。
- 有用なのは、推論論争が通常シェアードマップ無しのトレードオフ議論だから — どの技術が再配置でどれが拡張かを
  命名することは、実際のサービング構成判断の背にあるメンタルモデルであり、ゼロベンチマークの免責がそれを結果で
  なくメンタルモデルとして保つ。

## M4 Pro 設計図 —— ローカル LLM 市場の具体的な中間層（09-02）

- lws.io（HN 237）：常時稼働の M4 Pro Mac mini（48 GB）が **Qwen3.6-35B-A3B-OptiQ-4bit**（合計 35B /
  256 エキスパート、アクティブ約 3B、常駐約 20 GB）を主推論モデルとして実行し、チャットと整形には
  Gemma-4-E4B-it（2.4 GB）。**oMLX**（HF モデルブラウザ、自動検出、SSD 永続 KV キャッシュ）が提供し、実測で
  325 tok/s のプロンプト処理 / 34 tok/s の生成——iPhone、MacBook、mini から Tailscale 経由で到達。クライアント：
  Hermes エージェントバックエンド、iOS の Apollo、Raycast AI、コーディングには Pi。
- 数値はトレードオフと共に提示：4-bit OptiQ（敏感なレイヤーは 8-bit）は BF16 比で 1–2 ベンチマークポイントの
  コスト；デンス 27B モデルはスワップの苦痛なしには 16 GB マシンに収まらない；34 tok/s は「気にならない程度に速い」
  であって即時ではない。サイジングチェックリスト：ファイルサイズ ≈ 4-bit でのパラメータ数（GB）、そこから macOS の
  オーバーヘッド約 6–8 GB と KV キャッシュの余裕を引き、SSD スワップ前に 10–15% のバッファを残す。
- 位置づけ：slotstream のエキスパートストリーミング極端と API フォールバックの間にある具体的な中間——「GPT-5 や
  Claude Opus を必要としないリクエストの 80%」をカバーする約 $1,400 の常時稼働ボックスで、API は純粋主義の試金石
  ではなくフォールバックとして保持。示唆的な詳細は MoE の教訓：総パラメータ数はマーケティング、RAM に収まるのは
  アクティブパラメータ × 量子化。

## WebLLM —— ブラウザというゼロインストールの極端（09-03）

- mlc-ai/web-llm（18.8k★、Apache-2.0）が HN で再浮上（64 pts）：WebGPU による完全ブラウザ内の高性能 LLM 推論、
  サーバー不要——OpenAI 互換のストリーミング/JSON モード API、Web Worker + Service Worker 対応、Chrome 拡張としての
  デプロイ、Llama/Phi/Gemma/Mistral/Qwen2 の MLC 形式モデル。
- README 自身が示す正直な限界：最初のモデルロードはウェイトを未キャッシュのままダウンロード（「かなりの時間」）、
  関数呼び出しは「preliminary」、そして `model` チャットパラメータは**黙って無視される**——エンジンはリクエストごとでは
  なく構築時に選択される、OpenAI SDK コードを移植する人にとっての地雷。service worker はブラウザにいつでも殺されうる。
- このファイルの地図における位置：ローカル推論の最も強い*プライバシー*の端——ウェイトもプロンプトもタブの外へ出ない——
  そして M4 Pro 設計図や colibri のディスクストリーミングと同じ「デバイスへのフィット」トレンドのゼロインストール極端。
  トレードはハードウェアのサイジングの代わりにブラウザライフサイクルの脆さ。
